name: template_example
description: Example usage of package:template.
version: 0.1.0
environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  template:
    path: ../

flutter:
  uses-material-design: true
